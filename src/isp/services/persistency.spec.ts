describe('TESTANDO ALGUMA COISA', () => {
  it('descrição (IT)', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTANDO OUTRA COISA', () => {
  test('descricao de teste (TESTE)', () => {
    const nome = 'Luiz';
    expect(nome).toBe('Luiz');
  });
});
